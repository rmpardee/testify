describe('API integration', function(){
  var server, setupStub, JSONresponse;

  // before and after using sinon here

  it('todo.setup receives an array of todos when todo.init is called', function () {
  });
});
